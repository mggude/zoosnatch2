import React from "react";

export const ImageOneText = (props) => {
    return (
        <h2>{props.imageOneText}</h2>
    )
}

export const ImageTwoText = (props) => {
    return (
        <h2>{props.imageTwoText}</h2>
    )
}
